export interface City {
    name: string,
    population: number
    country: string // This is an ISo codeflo
    latitude: number
    longitude: number
}
export interface ErrorData {
    msg: string;
}